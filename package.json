{
  "name": "saas-shareanything",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "cd server && node index.js",
    "dev:all": "concurrently \"npm run dev\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^10.0.0",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
